import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 24,
  },
  scrollView: {
    gap: 20,
    paddingHorizontal: 10,
  },
  title: {
    fontSize: 16,
    color: "#000000",
    fontWeight: "600",
    paddingHorizontal: 10,
  },
});
